using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class zzERRORGEN : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int a = 5;
        print(a);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
